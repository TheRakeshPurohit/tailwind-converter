export type OutputView = "html" | "review" | "css" | "preview";

export type ReviewStatus =
  | "all"
  | "converted"
  | "approximated"
  | "unsupported"
  | "preserved"
  | "warnings";

export type PreviewViewport = "desktop" | "tablet" | "mobile";

export type PreviewMode = "split" | "original" | "converted";
